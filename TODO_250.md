# TDD/DRY Backlog (250 Items)

Each item follows a TDD loop (red -> green -> refactor) and a DRY rule (extract reusable helper before duplicating logic).

Legend:
- [ ] not started
- [x] completed

## Stream 1: Project governance and reproducibility scaffold
- [ ] 001. Define acceptance criteria and measurable success metric for "Project governance and reproducibility scaffold".
- [ ] 002. Add a failing unit test that encodes the core mathematical contract for "Project governance and reproducibility scaffold".
- [ ] 003. Add a failing property/invariance test for "Project governance and reproducibility scaffold" to prevent brittle overfit behavior.
- [ ] 004. Implement the smallest code path to make the core unit test pass for "Project governance and reproducibility scaffold".
- [ ] 005. Refactor shared code into a reusable helper/module to keep "Project governance and reproducibility scaffold" DRY.
- [ ] 006. Add an integration test on deterministic fixtures that exercises "Project governance and reproducibility scaffold" end-to-end.
- [ ] 007. Add regression test(s) protecting prior behavior while extending "Project governance and reproducibility scaffold".
- [ ] 008. Run benchmark/holdout comparison and store artifact JSON/MD for "Project governance and reproducibility scaffold".
- [ ] 009. Update operator/docs/paper notes with truthful findings (wins + failures) for "Project governance and reproducibility scaffold".
- [ ] 010. Close stream with checklist audit: reproducible command, seed, and artifact path for "Project governance and reproducibility scaffold".

## Stream 2: Deterministic split manager and holdout contract
- [ ] 011. Define acceptance criteria and measurable success metric for "Deterministic split manager and holdout contract".
- [ ] 012. Add a failing unit test that encodes the core mathematical contract for "Deterministic split manager and holdout contract".
- [ ] 013. Add a failing property/invariance test for "Deterministic split manager and holdout contract" to prevent brittle overfit behavior.
- [ ] 014. Implement the smallest code path to make the core unit test pass for "Deterministic split manager and holdout contract".
- [ ] 015. Refactor shared code into a reusable helper/module to keep "Deterministic split manager and holdout contract" DRY.
- [ ] 016. Add an integration test on deterministic fixtures that exercises "Deterministic split manager and holdout contract" end-to-end.
- [ ] 017. Add regression test(s) protecting prior behavior while extending "Deterministic split manager and holdout contract".
- [ ] 018. Run benchmark/holdout comparison and store artifact JSON/MD for "Deterministic split manager and holdout contract".
- [ ] 019. Update operator/docs/paper notes with truthful findings (wins + failures) for "Deterministic split manager and holdout contract".
- [ ] 020. Close stream with checklist audit: reproducible command, seed, and artifact path for "Deterministic split manager and holdout contract".

## Stream 3: Unified metrics schema and serialization
- [ ] 021. Define acceptance criteria and measurable success metric for "Unified metrics schema and serialization".
- [ ] 022. Add a failing unit test that encodes the core mathematical contract for "Unified metrics schema and serialization".
- [ ] 023. Add a failing property/invariance test for "Unified metrics schema and serialization" to prevent brittle overfit behavior.
- [ ] 024. Implement the smallest code path to make the core unit test pass for "Unified metrics schema and serialization".
- [ ] 025. Refactor shared code into a reusable helper/module to keep "Unified metrics schema and serialization" DRY.
- [ ] 026. Add an integration test on deterministic fixtures that exercises "Unified metrics schema and serialization" end-to-end.
- [ ] 027. Add regression test(s) protecting prior behavior while extending "Unified metrics schema and serialization".
- [ ] 028. Run benchmark/holdout comparison and store artifact JSON/MD for "Unified metrics schema and serialization".
- [ ] 029. Update operator/docs/paper notes with truthful findings (wins + failures) for "Unified metrics schema and serialization".
- [ ] 030. Close stream with checklist audit: reproducible command, seed, and artifact path for "Unified metrics schema and serialization".

## Stream 4: Baseline comparators and score calibration
- [ ] 031. Define acceptance criteria and measurable success metric for "Baseline comparators and score calibration".
- [ ] 032. Add a failing unit test that encodes the core mathematical contract for "Baseline comparators and score calibration".
- [ ] 033. Add a failing property/invariance test for "Baseline comparators and score calibration" to prevent brittle overfit behavior.
- [ ] 034. Implement the smallest code path to make the core unit test pass for "Baseline comparators and score calibration".
- [ ] 035. Refactor shared code into a reusable helper/module to keep "Baseline comparators and score calibration" DRY.
- [ ] 036. Add an integration test on deterministic fixtures that exercises "Baseline comparators and score calibration" end-to-end.
- [ ] 037. Add regression test(s) protecting prior behavior while extending "Baseline comparators and score calibration".
- [ ] 038. Run benchmark/holdout comparison and store artifact JSON/MD for "Baseline comparators and score calibration".
- [ ] 039. Update operator/docs/paper notes with truthful findings (wins + failures) for "Baseline comparators and score calibration".
- [ ] 040. Close stream with checklist audit: reproducible command, seed, and artifact path for "Baseline comparators and score calibration".

## Stream 5: Jacobi coordinate transforms and checks
- [ ] 041. Define acceptance criteria and measurable success metric for "Jacobi coordinate transforms and checks".
- [ ] 042. Add a failing unit test that encodes the core mathematical contract for "Jacobi coordinate transforms and checks".
- [ ] 043. Add a failing property/invariance test for "Jacobi coordinate transforms and checks" to prevent brittle overfit behavior.
- [ ] 044. Implement the smallest code path to make the core unit test pass for "Jacobi coordinate transforms and checks".
- [ ] 045. Refactor shared code into a reusable helper/module to keep "Jacobi coordinate transforms and checks" DRY.
- [ ] 046. Add an integration test on deterministic fixtures that exercises "Jacobi coordinate transforms and checks" end-to-end.
- [ ] 047. Add regression test(s) protecting prior behavior while extending "Jacobi coordinate transforms and checks".
- [ ] 048. Run benchmark/holdout comparison and store artifact JSON/MD for "Jacobi coordinate transforms and checks".
- [ ] 049. Update operator/docs/paper notes with truthful findings (wins + failures) for "Jacobi coordinate transforms and checks".
- [ ] 050. Close stream with checklist audit: reproducible command, seed, and artifact path for "Jacobi coordinate transforms and checks".

## Stream 6: Residual target mode and two-body subtraction
- [ ] 051. Define acceptance criteria and measurable success metric for "Residual target mode and two-body subtraction".
- [ ] 052. Add a failing unit test that encodes the core mathematical contract for "Residual target mode and two-body subtraction".
- [ ] 053. Add a failing property/invariance test for "Residual target mode and two-body subtraction" to prevent brittle overfit behavior.
- [ ] 054. Implement the smallest code path to make the core unit test pass for "Residual target mode and two-body subtraction".
- [ ] 055. Refactor shared code into a reusable helper/module to keep "Residual target mode and two-body subtraction" DRY.
- [ ] 056. Add an integration test on deterministic fixtures that exercises "Residual target mode and two-body subtraction" end-to-end.
- [ ] 057. Add regression test(s) protecting prior behavior while extending "Residual target mode and two-body subtraction".
- [ ] 058. Run benchmark/holdout comparison and store artifact JSON/MD for "Residual target mode and two-body subtraction".
- [ ] 059. Update operator/docs/paper notes with truthful findings (wins + failures) for "Residual target mode and two-body subtraction".
- [ ] 060. Close stream with checklist audit: reproducible command, seed, and artifact path for "Residual target mode and two-body subtraction".

## Stream 7: Legendre/Jacobi perturbation feature family
- [ ] 061. Define acceptance criteria and measurable success metric for "Legendre/Jacobi perturbation feature family".
- [ ] 062. Add a failing unit test that encodes the core mathematical contract for "Legendre/Jacobi perturbation feature family".
- [ ] 063. Add a failing property/invariance test for "Legendre/Jacobi perturbation feature family" to prevent brittle overfit behavior.
- [ ] 064. Implement the smallest code path to make the core unit test pass for "Legendre/Jacobi perturbation feature family".
- [ ] 065. Refactor shared code into a reusable helper/module to keep "Legendre/Jacobi perturbation feature family" DRY.
- [ ] 066. Add an integration test on deterministic fixtures that exercises "Legendre/Jacobi perturbation feature family" end-to-end.
- [ ] 067. Add regression test(s) protecting prior behavior while extending "Legendre/Jacobi perturbation feature family".
- [ ] 068. Run benchmark/holdout comparison and store artifact JSON/MD for "Legendre/Jacobi perturbation feature family".
- [ ] 069. Update operator/docs/paper notes with truthful findings (wins + failures) for "Legendre/Jacobi perturbation feature family".
- [ ] 070. Close stream with checklist audit: reproducible command, seed, and artifact path for "Legendre/Jacobi perturbation feature family".

## Stream 8: Shape-space invariants and hyperradius features
- [ ] 071. Define acceptance criteria and measurable success metric for "Shape-space invariants and hyperradius features".
- [ ] 072. Add a failing unit test that encodes the core mathematical contract for "Shape-space invariants and hyperradius features".
- [ ] 073. Add a failing property/invariance test for "Shape-space invariants and hyperradius features" to prevent brittle overfit behavior.
- [ ] 074. Implement the smallest code path to make the core unit test pass for "Shape-space invariants and hyperradius features".
- [ ] 075. Refactor shared code into a reusable helper/module to keep "Shape-space invariants and hyperradius features" DRY.
- [ ] 076. Add an integration test on deterministic fixtures that exercises "Shape-space invariants and hyperradius features" end-to-end.
- [ ] 077. Add regression test(s) protecting prior behavior while extending "Shape-space invariants and hyperradius features".
- [ ] 078. Run benchmark/holdout comparison and store artifact JSON/MD for "Shape-space invariants and hyperradius features".
- [ ] 079. Update operator/docs/paper notes with truthful findings (wins + failures) for "Shape-space invariants and hyperradius features".
- [ ] 080. Close stream with checklist audit: reproducible command, seed, and artifact path for "Shape-space invariants and hyperradius features".

## Stream 9: Symmetry-invariant polynomial features
- [ ] 081. Define acceptance criteria and measurable success metric for "Symmetry-invariant polynomial features".
- [ ] 082. Add a failing unit test that encodes the core mathematical contract for "Symmetry-invariant polynomial features".
- [ ] 083. Add a failing property/invariance test for "Symmetry-invariant polynomial features" to prevent brittle overfit behavior.
- [ ] 084. Implement the smallest code path to make the core unit test pass for "Symmetry-invariant polynomial features".
- [ ] 085. Refactor shared code into a reusable helper/module to keep "Symmetry-invariant polynomial features" DRY.
- [ ] 086. Add an integration test on deterministic fixtures that exercises "Symmetry-invariant polynomial features" end-to-end.
- [ ] 087. Add regression test(s) protecting prior behavior while extending "Symmetry-invariant polynomial features".
- [ ] 088. Run benchmark/holdout comparison and store artifact JSON/MD for "Symmetry-invariant polynomial features".
- [ ] 089. Update operator/docs/paper notes with truthful findings (wins + failures) for "Symmetry-invariant polynomial features".
- [ ] 090. Close stream with checklist audit: reproducible command, seed, and artifact path for "Symmetry-invariant polynomial features".

## Stream 10: Orbital-element feature extraction
- [ ] 091. Define acceptance criteria and measurable success metric for "Orbital-element feature extraction".
- [ ] 092. Add a failing unit test that encodes the core mathematical contract for "Orbital-element feature extraction".
- [ ] 093. Add a failing property/invariance test for "Orbital-element feature extraction" to prevent brittle overfit behavior.
- [ ] 094. Implement the smallest code path to make the core unit test pass for "Orbital-element feature extraction".
- [ ] 095. Refactor shared code into a reusable helper/module to keep "Orbital-element feature extraction" DRY.
- [ ] 096. Add an integration test on deterministic fixtures that exercises "Orbital-element feature extraction" end-to-end.
- [ ] 097. Add regression test(s) protecting prior behavior while extending "Orbital-element feature extraction".
- [ ] 098. Run benchmark/holdout comparison and store artifact JSON/MD for "Orbital-element feature extraction".
- [ ] 099. Update operator/docs/paper notes with truthful findings (wins + failures) for "Orbital-element feature extraction".
- [ ] 100. Close stream with checklist audit: reproducible command, seed, and artifact path for "Orbital-element feature extraction".

## Stream 11: Kozai-Lidov secular diagnostics
- [ ] 101. Define acceptance criteria and measurable success metric for "Kozai-Lidov secular diagnostics".
- [ ] 102. Add a failing unit test that encodes the core mathematical contract for "Kozai-Lidov secular diagnostics".
- [ ] 103. Add a failing property/invariance test for "Kozai-Lidov secular diagnostics" to prevent brittle overfit behavior.
- [ ] 104. Implement the smallest code path to make the core unit test pass for "Kozai-Lidov secular diagnostics".
- [ ] 105. Refactor shared code into a reusable helper/module to keep "Kozai-Lidov secular diagnostics" DRY.
- [ ] 106. Add an integration test on deterministic fixtures that exercises "Kozai-Lidov secular diagnostics" end-to-end.
- [ ] 107. Add regression test(s) protecting prior behavior while extending "Kozai-Lidov secular diagnostics".
- [ ] 108. Run benchmark/holdout comparison and store artifact JSON/MD for "Kozai-Lidov secular diagnostics".
- [ ] 109. Update operator/docs/paper notes with truthful findings (wins + failures) for "Kozai-Lidov secular diagnostics".
- [ ] 110. Close stream with checklist audit: reproducible command, seed, and artifact path for "Kozai-Lidov secular diagnostics".

## Stream 12: Takens embedding dataset builders
- [ ] 111. Define acceptance criteria and measurable success metric for "Takens embedding dataset builders".
- [ ] 112. Add a failing unit test that encodes the core mathematical contract for "Takens embedding dataset builders".
- [ ] 113. Add a failing property/invariance test for "Takens embedding dataset builders" to prevent brittle overfit behavior.
- [ ] 114. Implement the smallest code path to make the core unit test pass for "Takens embedding dataset builders".
- [ ] 115. Refactor shared code into a reusable helper/module to keep "Takens embedding dataset builders" DRY.
- [ ] 116. Add an integration test on deterministic fixtures that exercises "Takens embedding dataset builders" end-to-end.
- [ ] 117. Add regression test(s) protecting prior behavior while extending "Takens embedding dataset builders".
- [ ] 118. Run benchmark/holdout comparison and store artifact JSON/MD for "Takens embedding dataset builders".
- [ ] 119. Update operator/docs/paper notes with truthful findings (wins + failures) for "Takens embedding dataset builders".
- [ ] 120. Close stream with checklist audit: reproducible command, seed, and artifact path for "Takens embedding dataset builders".

## Stream 13: Local linear and local rational map models
- [ ] 121. Define acceptance criteria and measurable success metric for "Local linear and local rational map models".
- [ ] 122. Add a failing unit test that encodes the core mathematical contract for "Local linear and local rational map models".
- [ ] 123. Add a failing property/invariance test for "Local linear and local rational map models" to prevent brittle overfit behavior.
- [ ] 124. Implement the smallest code path to make the core unit test pass for "Local linear and local rational map models".
- [ ] 125. Refactor shared code into a reusable helper/module to keep "Local linear and local rational map models" DRY.
- [ ] 126. Add an integration test on deterministic fixtures that exercises "Local linear and local rational map models" end-to-end.
- [ ] 127. Add regression test(s) protecting prior behavior while extending "Local linear and local rational map models".
- [ ] 128. Run benchmark/holdout comparison and store artifact JSON/MD for "Local linear and local rational map models".
- [ ] 129. Update operator/docs/paper notes with truthful findings (wins + failures) for "Local linear and local rational map models".
- [ ] 130. Close stream with checklist audit: reproducible command, seed, and artifact path for "Local linear and local rational map models".

## Stream 14: Koopman/EDMD dictionary and ridge operator
- [ ] 131. Define acceptance criteria and measurable success metric for "Koopman/EDMD dictionary and ridge operator".
- [ ] 132. Add a failing unit test that encodes the core mathematical contract for "Koopman/EDMD dictionary and ridge operator".
- [ ] 133. Add a failing property/invariance test for "Koopman/EDMD dictionary and ridge operator" to prevent brittle overfit behavior.
- [ ] 134. Implement the smallest code path to make the core unit test pass for "Koopman/EDMD dictionary and ridge operator".
- [ ] 135. Refactor shared code into a reusable helper/module to keep "Koopman/EDMD dictionary and ridge operator" DRY.
- [ ] 136. Add an integration test on deterministic fixtures that exercises "Koopman/EDMD dictionary and ridge operator" end-to-end.
- [ ] 137. Add regression test(s) protecting prior behavior while extending "Koopman/EDMD dictionary and ridge operator".
- [ ] 138. Run benchmark/holdout comparison and store artifact JSON/MD for "Koopman/EDMD dictionary and ridge operator".
- [ ] 139. Update operator/docs/paper notes with truthful findings (wins + failures) for "Koopman/EDMD dictionary and ridge operator".
- [ ] 140. Close stream with checklist audit: reproducible command, seed, and artifact path for "Koopman/EDMD dictionary and ridge operator".

## Stream 15: Ulam transfer operator and occupancy forecasts
- [ ] 141. Define acceptance criteria and measurable success metric for "Ulam transfer operator and occupancy forecasts".
- [ ] 142. Add a failing unit test that encodes the core mathematical contract for "Ulam transfer operator and occupancy forecasts".
- [ ] 143. Add a failing property/invariance test for "Ulam transfer operator and occupancy forecasts" to prevent brittle overfit behavior.
- [ ] 144. Implement the smallest code path to make the core unit test pass for "Ulam transfer operator and occupancy forecasts".
- [ ] 145. Refactor shared code into a reusable helper/module to keep "Ulam transfer operator and occupancy forecasts" DRY.
- [ ] 146. Add an integration test on deterministic fixtures that exercises "Ulam transfer operator and occupancy forecasts" end-to-end.
- [ ] 147. Add regression test(s) protecting prior behavior while extending "Ulam transfer operator and occupancy forecasts".
- [ ] 148. Run benchmark/holdout comparison and store artifact JSON/MD for "Ulam transfer operator and occupancy forecasts".
- [ ] 149. Update operator/docs/paper notes with truthful findings (wins + failures) for "Ulam transfer operator and occupancy forecasts".
- [ ] 150. Close stream with checklist audit: reproducible command, seed, and artifact path for "Ulam transfer operator and occupancy forecasts".

## Stream 16: Symbolic dynamics and entropy-rate models
- [ ] 151. Define acceptance criteria and measurable success metric for "Symbolic dynamics and entropy-rate models".
- [ ] 152. Add a failing unit test that encodes the core mathematical contract for "Symbolic dynamics and entropy-rate models".
- [ ] 153. Add a failing property/invariance test for "Symbolic dynamics and entropy-rate models" to prevent brittle overfit behavior.
- [ ] 154. Implement the smallest code path to make the core unit test pass for "Symbolic dynamics and entropy-rate models".
- [ ] 155. Refactor shared code into a reusable helper/module to keep "Symbolic dynamics and entropy-rate models" DRY.
- [ ] 156. Add an integration test on deterministic fixtures that exercises "Symbolic dynamics and entropy-rate models" end-to-end.
- [ ] 157. Add regression test(s) protecting prior behavior while extending "Symbolic dynamics and entropy-rate models".
- [ ] 158. Run benchmark/holdout comparison and store artifact JSON/MD for "Symbolic dynamics and entropy-rate models".
- [ ] 159. Update operator/docs/paper notes with truthful findings (wins + failures) for "Symbolic dynamics and entropy-rate models".
- [ ] 160. Close stream with checklist audit: reproducible command, seed, and artifact path for "Symbolic dynamics and entropy-rate models".

## Stream 17: Recurrence quantification and topological gates
- [ ] 161. Define acceptance criteria and measurable success metric for "Recurrence quantification and topological gates".
- [ ] 162. Add a failing unit test that encodes the core mathematical contract for "Recurrence quantification and topological gates".
- [ ] 163. Add a failing property/invariance test for "Recurrence quantification and topological gates" to prevent brittle overfit behavior.
- [ ] 164. Implement the smallest code path to make the core unit test pass for "Recurrence quantification and topological gates".
- [ ] 165. Refactor shared code into a reusable helper/module to keep "Recurrence quantification and topological gates" DRY.
- [ ] 166. Add an integration test on deterministic fixtures that exercises "Recurrence quantification and topological gates" end-to-end.
- [ ] 167. Add regression test(s) protecting prior behavior while extending "Recurrence quantification and topological gates".
- [ ] 168. Run benchmark/holdout comparison and store artifact JSON/MD for "Recurrence quantification and topological gates".
- [ ] 169. Update operator/docs/paper notes with truthful findings (wins + failures) for "Recurrence quantification and topological gates".
- [ ] 170. Close stream with checklist audit: reproducible command, seed, and artifact path for "Recurrence quantification and topological gates".

## Stream 18: Shadowing-time and shadowing-error metrics
- [ ] 171. Define acceptance criteria and measurable success metric for "Shadowing-time and shadowing-error metrics".
- [ ] 172. Add a failing unit test that encodes the core mathematical contract for "Shadowing-time and shadowing-error metrics".
- [ ] 173. Add a failing property/invariance test for "Shadowing-time and shadowing-error metrics" to prevent brittle overfit behavior.
- [ ] 174. Implement the smallest code path to make the core unit test pass for "Shadowing-time and shadowing-error metrics".
- [ ] 175. Refactor shared code into a reusable helper/module to keep "Shadowing-time and shadowing-error metrics" DRY.
- [ ] 176. Add an integration test on deterministic fixtures that exercises "Shadowing-time and shadowing-error metrics" end-to-end.
- [ ] 177. Add regression test(s) protecting prior behavior while extending "Shadowing-time and shadowing-error metrics".
- [ ] 178. Run benchmark/holdout comparison and store artifact JSON/MD for "Shadowing-time and shadowing-error metrics".
- [ ] 179. Update operator/docs/paper notes with truthful findings (wins + failures) for "Shadowing-time and shadowing-error metrics".
- [ ] 180. Close stream with checklist audit: reproducible command, seed, and artifact path for "Shadowing-time and shadowing-error metrics".

## Stream 19: MDL ranking objective and candidate selector
- [ ] 181. Define acceptance criteria and measurable success metric for "MDL ranking objective and candidate selector".
- [ ] 182. Add a failing unit test that encodes the core mathematical contract for "MDL ranking objective and candidate selector".
- [ ] 183. Add a failing property/invariance test for "MDL ranking objective and candidate selector" to prevent brittle overfit behavior.
- [ ] 184. Implement the smallest code path to make the core unit test pass for "MDL ranking objective and candidate selector".
- [ ] 185. Refactor shared code into a reusable helper/module to keep "MDL ranking objective and candidate selector" DRY.
- [ ] 186. Add an integration test on deterministic fixtures that exercises "MDL ranking objective and candidate selector" end-to-end.
- [ ] 187. Add regression test(s) protecting prior behavior while extending "MDL ranking objective and candidate selector".
- [ ] 188. Run benchmark/holdout comparison and store artifact JSON/MD for "MDL ranking objective and candidate selector".
- [ ] 189. Update operator/docs/paper notes with truthful findings (wins + failures) for "MDL ranking objective and candidate selector".
- [ ] 190. Close stream with checklist audit: reproducible command, seed, and artifact path for "MDL ranking objective and candidate selector".

## Stream 20: MCTS archive policy and parent-selection tuning
- [ ] 191. Define acceptance criteria and measurable success metric for "MCTS archive policy and parent-selection tuning".
- [ ] 192. Add a failing unit test that encodes the core mathematical contract for "MCTS archive policy and parent-selection tuning".
- [ ] 193. Add a failing property/invariance test for "MCTS archive policy and parent-selection tuning" to prevent brittle overfit behavior.
- [ ] 194. Implement the smallest code path to make the core unit test pass for "MCTS archive policy and parent-selection tuning".
- [ ] 195. Refactor shared code into a reusable helper/module to keep "MCTS archive policy and parent-selection tuning" DRY.
- [ ] 196. Add an integration test on deterministic fixtures that exercises "MCTS archive policy and parent-selection tuning" end-to-end.
- [ ] 197. Add regression test(s) protecting prior behavior while extending "MCTS archive policy and parent-selection tuning".
- [ ] 198. Run benchmark/holdout comparison and store artifact JSON/MD for "MCTS archive policy and parent-selection tuning".
- [ ] 199. Update operator/docs/paper notes with truthful findings (wins + failures) for "MCTS archive policy and parent-selection tuning".
- [ ] 200. Close stream with checklist audit: reproducible command, seed, and artifact path for "MCTS archive policy and parent-selection tuning".

## Stream 21: Reservoir baseline and sparse-model distillation
- [ ] 201. Define acceptance criteria and measurable success metric for "Reservoir baseline and sparse-model distillation".
- [ ] 202. Add a failing unit test that encodes the core mathematical contract for "Reservoir baseline and sparse-model distillation".
- [ ] 203. Add a failing property/invariance test for "Reservoir baseline and sparse-model distillation" to prevent brittle overfit behavior.
- [ ] 204. Implement the smallest code path to make the core unit test pass for "Reservoir baseline and sparse-model distillation".
- [ ] 205. Refactor shared code into a reusable helper/module to keep "Reservoir baseline and sparse-model distillation" DRY.
- [ ] 206. Add an integration test on deterministic fixtures that exercises "Reservoir baseline and sparse-model distillation" end-to-end.
- [ ] 207. Add regression test(s) protecting prior behavior while extending "Reservoir baseline and sparse-model distillation".
- [ ] 208. Run benchmark/holdout comparison and store artifact JSON/MD for "Reservoir baseline and sparse-model distillation".
- [ ] 209. Update operator/docs/paper notes with truthful findings (wins + failures) for "Reservoir baseline and sparse-model distillation".
- [ ] 210. Close stream with checklist audit: reproducible command, seed, and artifact path for "Reservoir baseline and sparse-model distillation".

## Stream 22: Benchmark suites and strict holdout expansion
- [ ] 211. Define acceptance criteria and measurable success metric for "Benchmark suites and strict holdout expansion".
- [ ] 212. Add a failing unit test that encodes the core mathematical contract for "Benchmark suites and strict holdout expansion".
- [ ] 213. Add a failing property/invariance test for "Benchmark suites and strict holdout expansion" to prevent brittle overfit behavior.
- [ ] 214. Implement the smallest code path to make the core unit test pass for "Benchmark suites and strict holdout expansion".
- [ ] 215. Refactor shared code into a reusable helper/module to keep "Benchmark suites and strict holdout expansion" DRY.
- [ ] 216. Add an integration test on deterministic fixtures that exercises "Benchmark suites and strict holdout expansion" end-to-end.
- [ ] 217. Add regression test(s) protecting prior behavior while extending "Benchmark suites and strict holdout expansion".
- [ ] 218. Run benchmark/holdout comparison and store artifact JSON/MD for "Benchmark suites and strict holdout expansion".
- [ ] 219. Update operator/docs/paper notes with truthful findings (wins + failures) for "Benchmark suites and strict holdout expansion".
- [ ] 220. Close stream with checklist audit: reproducible command, seed, and artifact path for "Benchmark suites and strict holdout expansion".

## Stream 23: Factory UX, CLI ergonomics, and reporting
- [ ] 221. Define acceptance criteria and measurable success metric for "Factory UX, CLI ergonomics, and reporting".
- [ ] 222. Add a failing unit test that encodes the core mathematical contract for "Factory UX, CLI ergonomics, and reporting".
- [ ] 223. Add a failing property/invariance test for "Factory UX, CLI ergonomics, and reporting" to prevent brittle overfit behavior.
- [ ] 224. Implement the smallest code path to make the core unit test pass for "Factory UX, CLI ergonomics, and reporting".
- [ ] 225. Refactor shared code into a reusable helper/module to keep "Factory UX, CLI ergonomics, and reporting" DRY.
- [ ] 226. Add an integration test on deterministic fixtures that exercises "Factory UX, CLI ergonomics, and reporting" end-to-end.
- [ ] 227. Add regression test(s) protecting prior behavior while extending "Factory UX, CLI ergonomics, and reporting".
- [ ] 228. Run benchmark/holdout comparison and store artifact JSON/MD for "Factory UX, CLI ergonomics, and reporting".
- [ ] 229. Update operator/docs/paper notes with truthful findings (wins + failures) for "Factory UX, CLI ergonomics, and reporting".
- [ ] 230. Close stream with checklist audit: reproducible command, seed, and artifact path for "Factory UX, CLI ergonomics, and reporting".

## Stream 24: Paper-quality evidence synthesis and tables
- [ ] 231. Define acceptance criteria and measurable success metric for "Paper-quality evidence synthesis and tables".
- [ ] 232. Add a failing unit test that encodes the core mathematical contract for "Paper-quality evidence synthesis and tables".
- [ ] 233. Add a failing property/invariance test for "Paper-quality evidence synthesis and tables" to prevent brittle overfit behavior.
- [ ] 234. Implement the smallest code path to make the core unit test pass for "Paper-quality evidence synthesis and tables".
- [ ] 235. Refactor shared code into a reusable helper/module to keep "Paper-quality evidence synthesis and tables" DRY.
- [ ] 236. Add an integration test on deterministic fixtures that exercises "Paper-quality evidence synthesis and tables" end-to-end.
- [ ] 237. Add regression test(s) protecting prior behavior while extending "Paper-quality evidence synthesis and tables".
- [ ] 238. Run benchmark/holdout comparison and store artifact JSON/MD for "Paper-quality evidence synthesis and tables".
- [ ] 239. Update operator/docs/paper notes with truthful findings (wins + failures) for "Paper-quality evidence synthesis and tables".
- [ ] 240. Close stream with checklist audit: reproducible command, seed, and artifact path for "Paper-quality evidence synthesis and tables".

## Stream 25: Release gating, ablations, and replication package
- [ ] 241. Define acceptance criteria and measurable success metric for "Release gating, ablations, and replication package".
- [ ] 242. Add a failing unit test that encodes the core mathematical contract for "Release gating, ablations, and replication package".
- [ ] 243. Add a failing property/invariance test for "Release gating, ablations, and replication package" to prevent brittle overfit behavior.
- [ ] 244. Implement the smallest code path to make the core unit test pass for "Release gating, ablations, and replication package".
- [ ] 245. Refactor shared code into a reusable helper/module to keep "Release gating, ablations, and replication package" DRY.
- [ ] 246. Add an integration test on deterministic fixtures that exercises "Release gating, ablations, and replication package" end-to-end.
- [ ] 247. Add regression test(s) protecting prior behavior while extending "Release gating, ablations, and replication package".
- [ ] 248. Run benchmark/holdout comparison and store artifact JSON/MD for "Release gating, ablations, and replication package".
- [ ] 249. Update operator/docs/paper notes with truthful findings (wins + failures) for "Release gating, ablations, and replication package".
- [ ] 250. Close stream with checklist audit: reproducible command, seed, and artifact path for "Release gating, ablations, and replication package".

## Immediate execution order (first implementation tranche)
- [ ] Execute Streams 1-3 first (reproducibility, splits, metrics).
- [ ] Execute Streams 5-9 next (Jacobi/shape/symmetry feature breakthrough).
- [ ] Execute Streams 18-20 next (shadowing + MDL + MCTS policy).
- [ ] Run strict holdout + benchmark checks before any novelty claim.
- [ ] Update academic paper only with measured deltas and confidence intervals.
